# Core dependencies with version constraints
# Python 3.10+ required

# HTTP & Networking
requests>=2.28.0,<3.0
aiohttp>=3.8.0,<4.0
cloudscraper>=1.2.71,<2.0

# HTML Parsing
beautifulsoup4>=4.11.0,<5.0

# Plex Integration
PlexAPI>=4.13.0,<5.0

# String Matching
thefuzz>=0.20.0,<1.0

# Browser Automation (lazy-loaded for link resolution)
selenium>=4.8.0,<5.0
undetected-chromedriver>=3.5.0

# GUI (v1 QML interface — still functional)
PySide6>=6.7.0

# Desktop Notifications
plyer>=2.1.0,<3.0

# Utilities
python-dotenv>=1.0.0,<2.0

# JDownloader API (lazy-loaded, optional)
myjdapi>=1.1.6

# API Server (v2.0)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0

# Authentication (settable password for browser / self-hosted deployments)
bcrypt>=4.0.0,<5.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
