# Branch audit — 2026-08-19

**160 remote branches.** I had been carrying "about 11 stale branches" in my
head all session; that was off by an order of magnitude, which is itself the
finding — the branch picture has been unreliable, and I have been paying for it.
Twice today alone: a PR whose declared base was a lie, and another whose diff
was 90% line-ending noise.

Every branch below was classified by **content**, not by name and not by
ancestry alone. A branch can sit unmerged while its fix shipped by another
route, so "unmerged" never means "work not delivered".

## Summary

| class | count | what it means |
|---|---:|---|
| Merged into `main` | 70 | every commit is an ancestor of `main` |
| Unmerged but **zero diff** vs `main` | 1 | delivered by another route |
| Open PR | 7 | in flight — keep |
| Unmerged, docs/review only | 39 | no `backend/`, `frontend/` or `tests/` changes |
| Unmerged, touches code | 35 | needs a decision |

**71 branches are provably safe to delete** — their content is on `main` and
deleting the ref cannot lose anything.

## The 35 code branches, broken down

### The `review/int*-pr*` cluster — 19 branches, all 2026-07-19

`int-ci-pr3` … `int4-pr3`. Nineteen branches created on a single day, each
10–25 code files, all now 500+ commits behind `main`. These are CI integration
experiments from the July hardening push, not features. Several share merge
bases with each other rather than with `main` (git reports multiple merge bases
for them), which is what a fan of short-lived experiment branches looks like.

**Recommendation: delete.** Anything from that push that mattered is on `main`;
these are the scaffolding.

### The `agent/hybrid-sweep-*` trio — 2026-08-03 to 08-05

```text
agent/hybrid-sweep-implementation   103 files (65 code)
agent/hybrid-sweep-combined         116 files (69 code)
agent/hybrid-sweep-rebased          117 files (70 code)
```

Three variants of one piece of work, each a rebase/recombination of the last.
This is the largest unmerged body of work in the repo and the only entry here I
would not delete without looking at it properly. `-rebased` is the newest and
supersedes the other two.

**Recommendation: delete `-implementation` and `-combined`, keep `-rebased`
pending a proper read.**

### Recent, unmerged, no PR — worth a real decision

| branch | date | scope |
|---|---|---|
| `feat/item-history-sheet` | 08-16 | per-item history, the data half |
| `feat/queue-declared-semantics` | 08-16 | declare `affected_scope` per ScrapeCode, derive routing |
| `agent/turnstile-classification` | 08-10 | 12 code files; its own commits say "this branch is NOT the base" |
| `fix/dv-import-cadence` | 08-10 | import DV results during the walk, fail loudly |
| `agent/audit-fixes-pass2` | 08-05 | 47 code files |

`agent/turnstile-classification` explicitly records that it was superseded, so
it is a delete. The other four are real work that was simply never finished or
never proposed.

### Older code branches, superseded or abandoned

`agent/hdencode-lazy-hydration`, `agent/hdencode-off-switch`,
`fix/metadata-scan-cancellation`, `agent/cf-mitigated-challenge-signal`,
`agent/scan-failure-visibility` (+ its `-rebased` twin),
`agent/rename-safety-gate`, `agent/scan-metrics-wiring`,
`claude/nice-meitner-2b717b`, `claude/nostalgic-brattain-946f4f`.

All 07-19 to 08-05, all well behind `main`. The two `claude/*` names are
autogenerated session branches.

## The 39 docs-only branches

Peer-review reports, decision records and analyses, mostly `review/*` from
mid-July. They carry no code, so deleting them loses only the write-up. Most of
what mattered was folded into `docs/` on `main` or into memory.

Two are worth noting rather than deleting:

- **`feat/rss-coverage-canary`** (08-14) — despite the `feat/` name this is
  **docs only**: the canary design plus a build plan. Directly relevant to #61,
  and it confirms **the hybrid is not built**.
- **`docs/round10-request`** (today) — the live review request.

## What I am not doing

Deleting 71 refs is a bulk outward-facing change to a shared repository, so it
is your call, not mine. Nothing here has been deleted.

Recovery, if a deletion is ever regretted: for the 71, the commits are on `main`
and the ref can be recreated from any SHA. For the rest, a deleted branch's
commits survive in the remote's reflog for a limited window and in any local
clone that still has the ref — so the recoverable window is real but not
indefinite.

---

# The hybrid sweep, read properly — 2026-08-19

`agent/hybrid-sweep-rebased` is not a stale experiment. It is **118 commits
carried through sixteen peer-review rounds**, and none of it is on `main`.

## What it is

Its own governing document is `docs/reviews/2026-08-06-completion-contract-rev3.2.md`
— a completion contract with five stated rules (anti-proxy evidence; builder
never sole verifier; every claim bound to an exact SHA; a deferral must name
reason, residual risk and revisit trigger; a documented "do-not-promote"
completes a track) and two tracks of numbered exit criteria.

**Track 1 is RSS promotion.** R-1 through R-16.

| status | rows |
|---|---|
| ✅ done with evidence | R-2a |
| 🔨 built, verdict pending | R-1, R-3, R-4, R-5, R-6 |
| ⬜ open | R-2b, R-7 … R-16 |

The open rows are almost entirely operational and Jesse-gated: formal sign-off
(R-7), merge (R-8), one pinned build digest (R-9), deploy (R-11), a ~30h
bootstrap (R-12), a 7-day window at a fixed digest (R-13), then grading (R-14,
R-15) and a recorded decision (R-16).

So the **engineering** is essentially built and reviewed; what never happened is
the sign-off and everything downstream of it.

## What it adds

New subsystems, none of which exist on `main`:

```text
backend/release_grammar.py          529 lines changed
backend/sweep/session.py            356
backend/sweep/gate.py               355
backend/database.py                 872
+ 38 test files
```

Confirmed absent from `main`: `release_grammar.py`, `sweep/session.py`,
`sweep/gate.py`, `test_release_grammar.py`, `test_completed_row_feed_authority.py`.

## The problem

**Two independent RSS-promotion efforts exist and neither references the other.**

- `agent/hybrid-sweep-rebased` — last commit **2026-08-05**, 118 commits, 16
  review rounds, a full promotion contract with a gate module.
- **#61** `design/rss-readiness-gate` — designed **2026-08-11**, six days later.
  Searching its text for the sweep work returns nothing; the only "sweep" match
  is the ordinary English word.

#61 proposes a coverage-canary hybrid and states the hybrid is not built. The
sweep branch contains `backend/sweep/gate.py` and a demotion path (R-6:
"gate wired; demotion restores the safety net end-to-end"). Whether that is the
same gate, a compatible one, or a competing design is the question, and it is
not answerable from the documents alone.

## Cost of the delay

The branch is now **118 ahead / 347 behind `main`**. When the contract was
written it was *0 behind*. Everything merged since — the DV work, the queue
work, the media-kind stack — lands on top of it, and `backend/database.py` is
the single most contended file in the repo.

This does not expire, but it gets more expensive every week, and the review
evidence is bound to SHAs whose surrounding code has moved.

## Recommendation

Do **not** delete it, and do not silently keep carrying it either. It needs one
decision from Jesse: is RSS promotion proceeding via this contract, via #61's
canary hybrid, or not at all? Every path is defensible; carrying both is the
only option that is not.
