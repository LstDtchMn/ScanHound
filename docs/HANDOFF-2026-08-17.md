# ScanHound handoff — 2026-08-17

Written at the end of a very long weekend session, because the error rate in the
last few hours climbed and a clean restart is cheaper than continuing.

**Read the "How to work on this" section first. It is the most useful part.**

---

## 1. State of the world

`main` is at **`d08c989`**, deployed, and the running container is verified to
contain it. The full suite is green (5,102 passed at the last full run).

### Shipped and deployed this weekend

Five bugs that were live in production:

1. **Every reveal-stall crashed.** `_defer_item_only` wrote a `queue_reason` the
   column's CHECK rejects, so the code written to stop one bad item parking 78
   siblings had never once completed.
2. **Attempt records were fiction.** `_execute_inner` never closed an attempt, so
   the `finally` backstop was the only closer and all 3 production rows read
   `FAILED/attempt_not_closed` regardless of outcome.
3. **Attempts stamped in local time**, compared against sqlite's UTC in six
   queries. 4-hour skew on this host, and `'T' > ' '` so same-day rows sorted
   after any same-day cutoff.
4. **The promotion threshold was off by one** — `distinct_items_failing` could
   not see the item being classified, so a constant of 2 required 3.
5. **Scraper drift detection was inert** — `LAYOUT_CHANGED` and
   `REVEAL_CONTROL_ABSENT` never set `transport_attempted`, and the detector
   counts only rows where it is 1.

Three alerts that had **never once been able to fire**:

* `executor_starved` — dead behind THREE independent mixed-format timestamp
  comparisons, including during the 2026-08-13 stall it was built to catch.
* `source_no_progress` — fired on the first failed attempt ever, because a NULL
  last-progress fell back to the epoch.
* Both now use `julianday()` on every operand.

Features: source-global pacing (with human retries exempt), the retry-budget
deadlock exit, `Clear done` as one request instead of 563, the per-item history
data layer, and the source-level verification-hold card.

Also: **the database now has a backup.** It had none. A correct script had sat
unrunnable since Aug 15 because it was named `.ps1.NEW`, which PowerShell refuses
to execute. It is installed at
`C:\DockerData\infra-ops\scripts\scanhound-backup.ps1`, scheduled daily at 02:00
as SYSTEM, and **the restore was verified** — restored into a clean container,
integrity check ok, all six tables matching live exactly.

### Branches not merged

| Branch | State |
|---|---|
| `feat/source-hold-surface` | Merged to main already (`77d2a70`) |
| `feat/item-history-sheet` `1d9632f` | Data layer only; Svelte sheet NOT built |
| `feat/queue-declared-semantics` `9706fa0` | Restructuring pass 2, unreviewed |

---

## 2. Open work, highest value first

### A. Normalized-path collision fix — **DONE 2026-08-17**

> Built on branch `agent/dv-path-collision-fix`. Full write-up, measurements and
> peer-review rounds: `docs/reviews/peer-rounds/2026-08-17-dv-path-collision-fix.md`.
> **The characterisation below was WRONG and is corrected in place** — read the
> corrected paragraph, not the original claim, which is preserved only in git
> history. NOT merged and NOT deployed; both remain Jesse's.

`dv_labeler.build_index_and_paths()` does:

```python
for r in rows:
    p = normalize_path(r.get("path"), mappings)
    if p:
        idx[p] = r.get("dv_layer")        # last row wins, silently
        norm_to_path[p] = r.get("path")
```

**CORRECTED 2026-08-17.** The original text here said ~311 keys "all currently
resolve to the real layer — accidentally". That is not true, and neither was
the replacement I first wrote ("335 are currently wrong"). Both were snapshots
of a coin toss.

`get_dv_scans()` orders `last_seen_at DESC`, which looks like a tie-break and is
not one: a bulk rescan stamps thousands of rows inside one second. The live
table holds **6,948 scan rows across 5 distinct timestamps, 5,975 of them
sharing a single value**, so the "winner" under last-write-wins is whichever row
the sorter emits first among equals — and that shifts as rows are added. Running
the identical old-vs-new comparison twenty minutes apart, after 11 rows landed,
the count of keys whose layer changed went **335 → 0**.

The stable statement is the bound, not a point estimate:

```
normalized keys                                       4,725
keys with >1 row (every group is exactly 2)           2,223
keys where a real layer and a failed scan coexist       346   <- order decides
   of those, whose real layer produces a badge          180
keys with two DIFFERENT real layers (true conflict)       0
```

Measured at the consumer by running the real `sync_labels(dry_run=True)` against
live Plex three times, identical except row order: an adverse order matches 764
titles and applies **0** labels; a favourable order matches 1,079 and applies
**72 labels across 30 titles**. The fix lands on the favourable outcome by
construction. So the defect was never a wrong answer — it was an arbitrary one,
which is exactly why the number kept moving under measurement.

**The rule (from review, and do not deviate without re-reviewing):**

```
one unique authoritative layer + unknown(s)  -> that layer
all authoritative layers identical           -> that layer
zero authoritative layers                    -> unknown
multiple DIFFERENT authoritative layers      -> explicit CONFLICT, behave
                                                non-authoritatively (as unknown)
```

**"(as unknown)" was wrong, and peer review caught it.** Reusing `unknown` for a
conflict is unsafe on a MULTI-PART title: `pick_layer` runs its positive-rank
loop before its `unknown` handling, so a clean sibling part overrode the
conflict, made the title authoritative again, and re-enabled both label removal
and the `rating_key` back-write. The shipped rule uses a distinct
`LAYER_CONFLICT = "conflict"` and a new rule 0 in `pick_layer` — a contradicting
part poisons the title, checked before the rank loop. See §3 of the review doc.

* `none` is **authoritative** (detector ran, found no DV). `unknown` is failure.
* **Do NOT use `last_seen_at` to arbitrate two real layers.** `upsert_dv_scan()`
  lets a failed `unknown` scan preserve the old layer while advancing
  `last_seen_at`, so that timestamp does not date the layer. A `layer_observed_at`
  column would be needed first.
* **Do NOT use `_LAYER_RANK`.** That ranks parts of one Plex title, not two
  observations of one file.
* Choose a whole **row**, not a layer from one row and a raw path from another —
  `norm_to_path` feeds the `rating_key` back-annotation.
* Same pattern exists in `build_index()` and the `seed_index` comprehension. Fix
  all three with one shared aggregator; do not fix one and leave the others.

Tests required, **permutation invariance is the point**: `[fel, unknown]` and
`[unknown, fel]` must both give `fel`; `[fel, mel]` both ways must give conflict;
`norm_to_path` must follow the winning row; and a conflict must neither remove a
label nor back-write `rating_key`.

### B. The duplicate/best-version labelling feature (requested, not started)

The owner **keeps multiple versions deliberately** — this is a feature, not
cleanup. Plex reports **1,029 multi-version movies**:

```
1080+1080  474      4k+4k  451      1080+720  52
4k+4k+4k    34      1080+1080+1080  8      1080+4k  3
```

He wants the poster to indicate a duplicate exists, and the cover to carry the
**best** version's tags.

Key facts already measured:
* Resolution differs in only **6%** of multi-version movies. **Bitrate differs in
  95%** — that is the discriminator.
* `plex_cache` already holds `res`, `size`, `dovi`, `hdr`, `media_id`,
  `file_path` **per version**, so ranking can be local. No extra Plex calls.
* The DV *layer* comes from `dv_scan` via the normalized join — so **A must land
  first**.
* Review guidance: match at **version level**, never by `rating_key` alone. A
  1080p SDR and a 4K DV version share a movie id and need different tags.
* The existing HDR rule (`MAX(hdr) GROUP BY rating_key`) is the precedent for
  aggregation, and it fails safe on purpose.

### C. Smaller, verified, unstarted

* 118 `dv_scan` rows are **one file stored twice** (filesystem-confirmed:
  `Resolve-Path` maps both spellings to one canonical path). Insert-instead-of-
  update because storage identity is the raw string. **Not** a join problem —
  the consumer normalizes. Clean up *after* A, not before.
* 70 items are the same size+mtime under different mount roots. **Unresolved** —
  could be aliases or real copies. Do not merge on stored metadata.
* `feat/item-history-sheet` needs `ATTEMPT_HISTORY_TRUSTED_FROM` repointed from
  midnight to the actual deploy timestamp before it ships, or it will render
  pre-fix fabricated failures as real.
* Single-row `remove_package()` has the same epoch race the bulk path fixed.
* No automated test of the hold-card UI partition — the layer where the worst
  bug of the evening hid. Four browser cases listed in that branch's review doc.
* **`otherErr=1814`, source UNCERTAIN, UNINVESTIGATED.** The 2026-08-17 14:20
  run reported `+251 classified (1343 of 6926; unknown=5583, denied=0,
  otherErr=1814)`. What is *measured*: `dv_scan` has no error column, so that
  count comes from the task's own output, not the database; the layer counts in
  the DB agree with the monitor (`unknown=5583`), so the DB view is consistent;
  and `denied=0`, which is the number the 2026-08-15 ACL incident taught us to
  watch. What is *not known*: whether 1814 is growing, stable, or a running
  total — earlier runs today reported no `otherErr` field at all, so there is no
  baseline to compare against. `C:\DockerData\scanhound\dv-scan.log` is STALE
  (last written Aug 15) and contains 7,810 "Access is denied" lines, which per
  the 2026-08-15 incident were really `CreateProcess` on `dovi_tool.exe` failing,
  not the media files — do not read that log as current evidence. **First step:
  find which artifact the live task writes to, then get two consecutive
  `otherErr` values before concluding anything.**
* **CORRECTION to the item above, and a second finding (2026-08-17).**
  `ScanHound DV Detector` is **Disabled** (retired 8/15) — its "NextRun 2:20 PM"
  is the phantom schedule of a disabled task and never fires. So the monitor
  labelled "DV detector 2:20 PM run" is watching a task that CANNOT run, and its
  `otherErr=1814` did not come from that task. Find what that monitor actually
  reads before treating the number as being about anything in particular.

  Separately, and MEASURED: `ScanHound-DVScan` (the surviving 4-hourly task, and
  the one making real progress) has `LastTaskResult = 0x800710E0` — non-zero —
  while its monitor reports healthy progress and `denied=0`. Windows would not
  resolve that code to a message. **INFERRED, not measured:** it is consistent
  with a trigger firing while the previous instance is still running, which would
  fit a 4-hourly task whose run exceeds four hours; the task was still `Running`
  at 08:2x having started 07:00. Testable by timing one full run. Do that before
  believing the inference — a non-zero result beside "healthy progress" is exactly
  the shape of a signal that means something other than it appears to.
* `ScanHound Qualification Evidence` scheduled task returns exit 3. Untouched.
* `ScanHound-MountNASShares` fails every 12 min — now LOGGED, and the cause is
  the host-side check on `nas-tv-blackbeard` failing while the container's
  `/library/tv` is genuinely fine. Guard crying wolf; root cause unfixed.

---

## 3. How to work on this

**This section exists because of the mistakes below. Read it before the mistakes.**

### Verify against the CONSUMER, always

Before reporting any measurement, run it through the **actual production
function**, not a reimplementation in a throwaway query. Every error in section 4
would have been caught by this one rule.

```
WRONG:  SELECT ... WHERE path LIKE '%4k%'        (my guess at what the code does)
RIGHT:  from backend.rename.dv_paths import normalize_path   (what it does)
```

### State confidence explicitly

Mark every claim **measured / inferred / assumed**. If it is inferred, say what
would falsify it. A number with no provenance is an opinion.

### Peer review BEFORE reporting, not after

Two findings this weekend reached the owner as fact and were retracted. If a
finding is going to change what he does, get it checked first. He relays to
ChatGPT by hand; the loop costs him a copy-paste and it has caught five real
things.

### Smaller steps, more checkpoints

Long investigation chains compound an early wrong turn. Stop and confirm.

### Owner-specific, non-negotiable

* **He is not a programmer.** Plain language, expand jargon, lead with what it
  means for him. Plain is not vague — keep real numbers.
* **Every question and every decision that is his goes in an AskUserQuestion**,
  recommendation first, with the downside stated.
* **Jesse-only:** merge, deploy, force-push, mark-ready, prod settings, enable.
* **Never repurpose a credential found on disk without asking** — every time.
* He is usually on **Android**. `SendUserFile` never arrives; links and
  pasteable text only.
* `docker-compose.yml` carries the DV ingest key and `127.0.0.1:9721:9721` as
  **uncommitted** edits. `git add -A` sweeps them in; `git stash` swallowed them
  once. Check `grep -c '9721:9721'` after any git operation.

### Environment traps that cost time this weekend

* `docker` writes to stderr, so **PowerShell 5.1 reports failure on success**.
  Never check `$?` — verify the outcome.
* A `git push` run from the wrong branch **reports success and moves nothing**.
  Verify with `git ls-remote`.
* Backticks in a `git commit -m` string get executed. Use `--file=-` with a
  quoted heredoc.
* Mutation-testing harnesses must purge `__pycache__` between mutants — two
  same-size writes in one second reuse stale bytecode and read as SURVIVED.
* The API has **no `/api` prefix**. Routes are `/download/...`. `/api/...` falls
  through to the SPA catch-all and returns 405, which looks like an auth error.
* `schtasks` is not visible to a non-elevated shell — it returns Access Denied,
  which looks identical to "not registered".

---

## 4. Mistakes I made, so they are not repeated

All four are the same shape: **I measured a proxy for the consumer instead of the
consumer.**

1. **"Zero items at different resolutions."** I classified resolution by
   searching for `4k` in the path. Every scanned file lives under a folder named
   `4K Columbo` / `4K Drives`, so all 10,548 rows were labelled 4K — including
   ones whose filenames say `1080p`. The output printed a negative count and I
   did not stop.
2. **"The dv_label_vocab setting is a live mismatch."** I read the config field
   and assumed it flowed through. The loader filters any value not in
   `_LAYER_LABELS` and merges over defaults, specifically so a typo cannot strip
   labels. The setting is inert. **No change needed.**
3. **"577 lost DV detections, 42 lost FEL."** Graded HIGH by peer review on the
   strength of my numbers, then withdrawn. I measured raw string equality;
   `normalize_path()` already unifies separators, casefolds AND rewrites drive/UNC
   roots, and both sides go through it.
4. **"The hold release needs a login."** It returned 405, which I read as auth.
   It was my wrong URL. The conclusion happened to be right for the wrong reason,
   which is worse than being plainly wrong.

Also worth carrying: **four separate tests this weekend verified nothing** — a
fixture whose INSERT silently failed so assertions ran against an empty table, a
test asserting on a docstring, a modelled barrier test that reproduced the
locking pattern instead of exercising it, and a JD double that returned `None` on
its success path so neither direction of the contract was pinned. Mutation
testing caught three of the four. **Run it.**

---

## 5. Immediate next actions

1. Nothing is broken. Nothing is blocked. No fire.
2. The entrypoint fix (`d08c989`) is on main but the **running container still
   has the old one** — it takes effect on the next rebuild. Not urgent; it only
   matters at restart.
3. ~~Start with **section 2A**.~~ **DONE 2026-08-17** on
   `agent/dv-path-collision-fix` — pushed, NOT merged, NOT deployed. It went
   through two peer-review rounds; round 1 returned REQUEST CHANGES on a real
   MEDIUM blocker. Read
   `docs/reviews/peer-rounds/2026-08-17-dv-path-collision-fix.md` before
   touching it.
4. **Next: 2B**, the duplicate/best-version labelling the owner actually asked
   for. 2A was its prerequisite and is now in place (pending merge).
5. **Also queued: a working notification channel.** ScanHound currently has
   **zero** outbound channels — headless container disables plyer, and Discord/
   Slack/Pushover/generic webhook are all unset, so `send_notification()` is a
   no-op. The DV conflict alert therefore ships on the in-app websocket. The
   owner asked for Gotify; that needs a dedicated channel (header auth, correct
   payload, 0–10 priority mapping), not just a settings entry. Scoped in §5b of
   the review doc.
6. Standing lesson from this round, worth more than the fix: **three separate
   pieces of code were correct and could not deliver** — a conflict state a
   sibling part silently overrode, an alert aimed at a NotificationManager with
   no channels, and an alert aimed at the wrong manager entirely. Ask whether a
   thing is *delivered*, not whether it is *called*.
