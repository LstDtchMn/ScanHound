# Secret-scanning config (Track C, contract C-1).
#
# OWNERSHIP: Jesse (arbiter) owns every allowlist entry; Claude proposes,
# ChatGPT reviews. Adding an entry requires: the finding's fingerprint, why it
# is a false positive, and this header updated. An entry without a reason is
# a defect, not a suppression.

[extend]
useDefault = true

[allowlist]
description = "Reviewed false positives - 2026-07-31 history-wide review (703 commits, 2 findings, both manually classified FP)"
# The only two findings ever recorded: the PUBLIC GPG signing-key fingerprint
# of the official Python Docker image maintainer, matched by generic-api-key
# inside two committed `docker inspect` evidence captures. A public key
# fingerprint is not a secret; the files are frozen review evidence.
# Fingerprints (report 11-gitleaks-report.json, infra-ops security dir):
#   079f7ec6...:docs/feature-pack-review/qualification-evidence/old-image-inspect.json:generic-api-key:20
#   079f7ec6...:docs/feature-pack-review/qualification-evidence/new-image-inspect.json:generic-api-key:19
paths = [
    '''docs/feature-pack-review/qualification-evidence/old-image-inspect\.json''',
    '''docs/feature-pack-review/qualification-evidence/new-image-inspect\.json''',
]
